npm install
npm start

git status
git add .
git commit -m "quick commit"
git push

To Do

Setup Schema in Supabase

users - user_id, name, email

driving_sessions - session_id, session_duration, sharp_turns/sverves, screen_duration, sudden_breaks